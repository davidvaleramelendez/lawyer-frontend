// You can customize the Authentication login template

const authenticationConfig = {
  LoginTemplate: {
    basic: false,
    cover: false,
    simple: true
  }
}

export default authenticationConfig
